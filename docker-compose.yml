services:
  postgres:
    image: pgvector/pgvector:pg17
    environment:
      POSTGRES_USER: foothold
      POSTGRES_PASSWORD: foothold
      POSTGRES_DB: foothold
    ports:
      - "54329:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata: {}
